rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // This rule allows anyone with your Firestore database reference to view, edit,
    // and delete all data in your Firestore database. It is useful for getting
    // started, but it is configured to expire after 30 days because it
    // leaves your app open to attackers. At that time, all client
    // requests to your Firestore database will be denied.
    //
    // Make sure to write security rules for your app before that time, or else
    // all client requests to your Firestore database will be denied until you Update
    // your rules
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2027, 7, 30);
    }

    // ===========================================
    // FEEDBACK / FEATURE REQUEST SYSTEM (added with the Batch 1+2
    // rollout). This app has a lightweight Firebase Auth session layered under the
    // existing employee-code + PIN flow, so signed-in app sessions can
    // use this collection without leaving it public.
    match /featureRequests/{requestId} {
      allow read, write: if request.auth != null;
    }

    // ===========================================
    // VIOLATIONS LOG (violations.js) + shared "system" config docs
    // (also used by monthlystats.js's GitHub backup config). Same
    // reasoning as featureRequests above — require the lightweight
    // Firebase Auth session created after employee-code + PIN login.
    // ===========================================
    match /violationLogs/{logId} {
      allow read, write: if request.auth != null;
    }
    match /violationsArchive/{month} {
      allow read, write: if request.auth != null;
    }
    match /system/{docId} {
      allow read, write: if request.auth != null;
    }

    // ===========================================
    // SHIFT MANAGEMENT (shiftmanagement.js) — the new configurable,
    // Firestore-backed shift system. Same reasoning as the other
    // collections above: require the lightweight Firebase Auth session
    // created after employee-code + PIN login.
    // ===========================================
    match /shifts/{shiftId} {
      allow read, write: if request.auth != null;
    }

    // ===========================================
    // NOTIFICATION CENTER (notificationcenter.js, Phase 1) — persistent
    // per-user notification history. Same reasoning as the other
    // collections above: require the lightweight Firebase Auth session
    // created after employee-code + PIN login.
    // ===========================================
    match /notificationHistory/{entryId} {
      allow read, write: if request.auth != null;
    }
  }
}
